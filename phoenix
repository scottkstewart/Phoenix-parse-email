#!/usr/bin/env sh

while [ "$1" != "" ]; do
    case $1 in
        -h )                echo "USAGE: phoenix [-h] {add, set, run}
-h: print usage and exit
add: add/change user
set: set interval between checks
run: run checks"
                            exit 1
                            ;;
        add )           	exec ~/.PPE/add.py
                                ;;
        set )    		exec ~/.PPE/setInterval.py
                                ;;
        run )           	exec ~/.PPE/run.py
				exit 1
    esac
    shift
done
