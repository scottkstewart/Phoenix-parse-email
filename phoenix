#!/usr/bin/env sh

while [ "$1" != "" ]; do
    case $1 in
        -h )                echo "USAGE: phoenix [-h] {add, set, run}
-h: print usage and exit
add: add/change user
set: set interval between checks
run: run checks"
                            exit 1
                            ;;
        add )           	exec ~/Phoenix-parse-email/add.py
                                ;;
        set )    		    exec ~/Phoenix-parse-email/setInterval.py
                                ;;
        run )           	exec ~/Phoenix-parse-email/run.py
				exit 1
    esac
    shift
done
